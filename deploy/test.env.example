# No business configuration is read from environment files.
# Restore data/novatest.db from a controlled novatest-data-*.tar.gz package
# before starting the application. The deployment port is fixed at 18084.
